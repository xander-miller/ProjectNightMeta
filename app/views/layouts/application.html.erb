<!DOCTYPE html>
<html>
  <head>
    <title>Meetup Project Night Meta</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link rel="stylesheet" href="<%= stylesheet_path 'bootstrap.min' %>" media="screen"/>
    <link rel="stylesheet" href="<%= stylesheet_path 'bootstrap-theme.min' %>" media="screen"/>
    <%= stylesheet_link_tag    "application", media: "screen" %>
    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="../../assets/js/html5shiv.js"></script>
      <script src="../../assets/js/respond.min.js"></script>
    <![endif]-->
    <%= csrf_meta_tags %>
  </head>
  <body>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Project Night Meta</a>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li><a href="/about">About</a></li>
          <li class="signin-nav-gap"> &nbsp; </li>
          <li><%- if session[:mu_id] -%>
      <%= link_to session[:mu_name], '/signout', {title: 'Sign out'} -%>
    <%- else -%>
      <%= link_to 'Sign in', '/login' %>
    <%- end -%></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>

  <div class="container">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </div><!-- /.container -->

  <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
  <script src="<%= javascript_path 'jquery-2.0.3.min' %>"></script>
  <!-- Include all compiled plugins (below), or include individual files as needed -->
  <script src="<%= javascript_path 'bootstrap.min' %>"></script>
  <%= javascript_include_tag "application" %>
  </body>
</html>

